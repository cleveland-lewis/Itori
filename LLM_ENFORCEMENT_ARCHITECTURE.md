# LLM Enforcement System Architecture

## Overview

This document provides a visual and conceptual map of the LLM enforcement system.

---

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Application Layer                            │
│  (Views, Services, Features — Cannot call providers directly)      │
└─────────────────────────────────────────┬───────────────────────────┘
                                          │
                        AIEngine.request(Port, Input)
                                          │
                                          ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     AIEngine._executeWithGuards                      │
│                    ═══════════════════════════════════                │
│              🛡️  SINGLE ENFORCEMENT POINT (Lines 67-117)  🛡️        │
│                    ═══════════════════════════════════                │
│                                                                       │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │ if !enableLLMAssistance {                                     │  │
│  │     ✅ Record suppression                                     │  │
│  │     ✅ Log audit entry (reasonCode: .llmDisabled)            │  │
│  │     ✅ Execute fallback ONLY                                 │  │
│  │     ✅ Return result (provenance: .fallback)                 │  │
│  │     ❌ NO provider selection                                 │  │
│  │     ❌ NO network requests                                   │  │
│  │     return result                                             │  │
│  │ }                                                              │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  ⚙️  Toggle is ON: Continue to provider selection...                │
└─────────────────────────────────────────┬───────────────────────────┘
                                          │
                            (Toggle ON only)
                                          │
                                          ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       Provider Selection Logic                       │
│  - Check provider availability                                       │
│  - Select best provider for port                                     │
│  - Apply privacy redaction                                           │
│  - Execute time budget                                               │
└─────────────────────────────────────────┬───────────────────────────┘
                                          │
                                          ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    provider.execute() Call                           │
│                                                                       │
│  🐛 DEBUG Canary (Lines 359-390):                                   │
│     if !enableLLMAssistance { assertionFailure(...) }               │
│     (Crashes dev builds if invariant violated)                       │
│                                                                       │
│  📡 Actual network request to LLM provider                          │
└─────────────────────────────────────────┬───────────────────────────┘
                                          │
                                          ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       Return Result                                  │
│  - Provenance: .provider(id)                                         │
│  - Diagnostic with latency, reason codes                             │
│  - Health monitor: providerAttemptCount++                            │
│  - Audit log: eventType = .providerAttempt                          │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Enforcement Layers (Defense in Depth)

```
┌─────────────────────────────────────────────────────────────────────┐
│ Layer 1: CODE GATE (Runtime)                                        │
│ ────────────────────────────────────────────────────────────────    │
│ Location: AIEngine._executeWithGuards (lines 67-117)                │
│ When: Every request                                                  │
│ What: Checks toggle, skips providers if OFF                          │
│ Defense: Prevents provider calls at source                           │
└─────────────────────────────────────────────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Layer 2: DEBUG CANARY (Development)                                 │
│ ────────────────────────────────────────────────────────────────    │
│ Location: Before provider.execute() (lines 359-390)                 │
│ When: DEBUG builds only                                              │
│ What: Assertion fires if provider called with toggle OFF             │
│ Defense: Catches violations during development                       │
└─────────────────────────────────────────────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Layer 3: CI TESTS (Pre-Merge)                                       │
│ ────────────────────────────────────────────────────────────────    │
│ Location: Tests/LLMToggleEnforcementTests.swift                     │
│ When: Every commit, every PR                                         │
│ What: Proves toggle OFF → 0 provider attempts                       │
│ Defense: Blocks merge if invariant violated                          │
└─────────────────────────────────────────────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Layer 4: STATIC ANALYSIS (Pre-Merge)                                │
│ ────────────────────────────────────────────────────────────────    │
│ Location: .github/workflows/llm-enforcement.yml                     │
│ When: Every commit, every PR                                         │
│ What: Checks for architectural violations                            │
│ Defense: Warns about direct provider calls, missing comments         │
└─────────────────────────────────────────────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Layer 5: HEALTH MONITOR (Runtime Observable)                        │
│ ────────────────────────────────────────────────────────────────    │
│ Location: SharedCore/AIEngine/Core/HealthMonitor.swift              │
│ When: Always                                                         │
│ What: Counters track provider attempts, suppressions                 │
│ Defense: Observable proof of enforcement                             │
└─────────────────────────────────────────────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Layer 6: AUDIT LOG (Runtime Traceable)                              │
│ ────────────────────────────────────────────────────────────────    │
│ Location: SharedCore/AIEngine/Core/AIAuditLog.swift                 │
│ When: Always                                                         │
│ What: Event log with provenance, reason codes                        │
│ Defense: Traceable history, forensic analysis                        │
└─────────────────────────────────────────────────────────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Layer 7: QA CHECKLIST (Pre-Release)                                 │
│ ────────────────────────────────────────────────────────────────    │
│ Location: Docs/PRODUCTION_VERIFICATION_CHECKLIST_LLM.md             │
│ When: Every release                                                  │
│ What: Manual verification of counters                                │
│ Defense: Human verification before ship                              │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow (Toggle OFF)

```
User Action
    │
    ▼
Feature/View calls AIEngine.request(DurationEstimationPort, input)
    │
    ▼
AIEngine._executeWithGuards()
    │
    ├─ Check: enableLLMAssistance == false? YES
    │
    ├─ Action: Record suppression
    │   └─ healthMonitor.recordLLMSuppression("llm_toggle_disabled")
    │   └─ suppressedByLLMToggleCount++
    │
    ├─ Action: Log audit entry
    │   └─ auditLog.log(eventType: .suppressed, reasonCode: .llmDisabled)
    │
    ├─ Action: Execute fallback ONLY
    │   └─ fallback.executeFallback(DurationEstimationPort, input)
    │   └─ healthMonitor.recordFallbackOnly()
    │   └─ fallbackOnlyCount++
    │
    ├─ Action: Return result
    │   └─ AIResult(provenance: .fallback, ...)
    │
    └─ SKIP: Provider selection, provider.execute(), network
    
    ▼
Result returned to caller
    │
    ▼
Observable State:
    providerAttemptCountTotal = 0  ✅
    suppressedByLLMToggleCount > 0  ✅
    fallbackOnlyCount > 0  ✅
```

---

## Data Flow (Toggle ON)

```
User Action
    │
    ▼
Feature/View calls AIEngine.request(DurationEstimationPort, input)
    │
    ▼
AIEngine._executeWithGuards()
    │
    ├─ Check: enableLLMAssistance == true? YES
    │
    ├─ Action: Continue to provider selection
    │   └─ Select best provider for port
    │   └─ Apply privacy redaction
    │   └─ Execute time budget
    │
    ├─ Action: Call provider.execute()
    │   └─ (DEBUG Canary: verify toggle still ON)
    │   └─ Network request to LLM service
    │   └─ healthMonitor.recordProviderAttempt()
    │   └─ providerAttemptCountTotal++
    │
    ├─ Action: Log audit entry
    │   └─ auditLog.log(eventType: .providerAttempt, providerID: "openai")
    │
    └─ Action: Return result
        └─ AIResult(provenance: .provider("openai"), ...)
    
    ▼
Result returned to caller
    │
    ▼
Observable State:
    providerAttemptCountTotal > 0  ✅ (Expected when toggle ON)
    suppressedByLLMToggleCount = 0  ✅
    fallbackOnlyCount = 0  ✅
```

---

## Verification Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│ Development                                                          │
│ ───────────────────────────────────────────────────────────────     │
│ 1. Write code                                                        │
│ 2. DEBUG canary catches violations → assertion failure              │
│ 3. Fix bug                                                           │
│ 4. Run LLMToggleEnforcementTests locally                             │
│ 5. Push to PR                                                        │
└─────────────────────────┬───────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────────┐
│ CI (Pre-Merge)                                                       │
│ ───────────────────────────────────────────────────────────────     │
│ 1. GitHub Actions runs on PR                                         │
│ 2. Executes LLMToggleEnforcementTests                                │
│ 3. Runs static analysis                                              │
│ 4. Pass → Allow merge                                                │
│ 5. Fail → Block merge, comment on PR                                │
└─────────────────────────┬───────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────────┐
│ QA (Pre-Release)                                                     │
│ ───────────────────────────────────────────────────────────────     │
│ 1. Build Release configuration                                       │
│ 2. Disable LLM toggle                                                │
│ 3. Use app heavily (30+ minutes)                                     │
│ 4. Check counters in Developer Settings                              │
│ 5. Provider Attempts = 0? → PASS, can ship                          │
│ 6. Provider Attempts > 0? → FAIL, DO NOT SHIP                       │
└─────────────────────────┬───────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Production                                                           │
│ ───────────────────────────────────────────────────────────────     │
│ 1. User disables toggle                                              │
│ 2. Code gate enforces fallback-only                                  │
│ 3. Health monitor: providerAttemptCount = 0                          │
│ 4. Audit log: all entries are .suppressed or .fallback              │
│ 5. Privacy guarantee intact ✅                                       │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Key Takeaways

1. **Single Enforcement Point:** `_executeWithGuards` is the ONLY place where providers can be called
2. **Early Check:** Toggle check happens BEFORE any provider logic
3. **Layered Defense:** 7 independent verification mechanisms
4. **Observable:** Health monitor and audit log provide runtime proof
5. **Regression-Proof:** CI blocks violations, DEBUG catches bugs, QA verifies before ship

---

## References

- **Full Documentation:** `Docs/Architecture/LLM_ENFORCEMENT_INVARIANT.md`
- **Quick Reference:** `LLM_ENFORCEMENT_QUICK_REFERENCE.md`
- **Implementation Summary:** `LLM_ENFORCEMENT_LOCK_IN_COMPLETE.md`
- **QA Checklist:** `Docs/PRODUCTION_VERIFICATION_CHECKLIST_LLM.md`
